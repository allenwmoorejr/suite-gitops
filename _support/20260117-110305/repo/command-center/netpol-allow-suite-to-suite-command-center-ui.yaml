apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-suite-to-suite-command-center-ui
  namespace: suite
spec:
  podSelector:
    matchLabels:
      app: suite-command-center-ui
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: suite
      ports:
        - protocol: TCP
          port: 80
