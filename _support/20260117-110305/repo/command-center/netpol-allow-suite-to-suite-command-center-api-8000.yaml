apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-suite-to-suite-command-center-api-8000
  namespace: suite
spec:
  podSelector:
    matchLabels:
      app: suite-command-center-api
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: suite
      ports:
        - protocol: TCP
          port: 8000
