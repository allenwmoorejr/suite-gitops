apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Flux bootstrap (controllers + sync)
  - ./flux-system

  # Apps — include each app ONCE
  - ../../apps/suite/command-center
